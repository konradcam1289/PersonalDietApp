import { View, StyleSheet, ImageBackground } from "react-native";


function BackgroundImage() {
    return (
        <View>
            <BackgroundImage source={require('../Image/obraz1.png')} />
        </View>
    )
}

export default BackgroundImage;

const styles = StyleSheet.create({

})